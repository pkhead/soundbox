{
  "name": "soundbox",
  "version": "1.0.0",
  "description": "",
  "gypfile": true,
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build-shell": "tsc -p src",
    "build-app": "tsc -p app-src",
    "build-worker": "tsc -p worker-src",
    "build-app-swc": "swc app-src --out-file app-out",
    "build": "npm run build-shell && npm run build-app && npm run build-worker"
  },
  "author": "pkhead",
  "license": "MIT",
  "devDependencies": {
    "@swc/cli": "^0.1.62",
    "@swc/core": "^1.3.49",
    "@types/audioworklet": "^0.0.41",
    "electron": "^24.1.0",
    "electron-rebuild": "^3.2.9"
  },
  "dependencies": {
    "naudiodon": "^2.3.6",
    "node-addon-api": "^6.0.0",
    "speaker": "^0.5.4",
    "typescript": "^5.0.4"
  }
}
